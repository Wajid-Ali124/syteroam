import DemoClient from './DemoClient';

export const metadata = {
  title: "Book a Demo — SyteRoam | SyteLine ERP Mobile App",
  description: "Book a personalised demo of SyteRoam — the native SyteLine ERP mobile app for manufacturing and service teams. See real workflows built for your industry.",
};

export default function DemoPage() {
  return <DemoClient />;
}
